This tool gets the list of nodes (URLs), and fetches the node graph
from URL links.

                        Hitoshi Yamauchi
                        2012-5-20(Sun)

Install BeautifulSoup4

  sudo apt-get install python-setuptools
  sudo easy_install beautifulsoup4

